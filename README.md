# HomeLedger

Private local budgeting. See branch `cursor/homeledger-rebuild-2f3c` for the full app (open the PR).

Quick start after merge:

```powershell
copy .env.example .env
docker compose up --build
```
